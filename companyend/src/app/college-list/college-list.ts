import { Component } from '@angular/core';

// Placeholder — college list removed in single-page flow.
@Component({
  standalone: true,
  selector: 'college-list',
  template: `<div style="padding:1rem">College list removed — not used in the single page app.</div>`
})
export class CollegeList { }
